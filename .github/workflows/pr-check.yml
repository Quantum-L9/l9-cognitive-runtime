name: pr-check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Sync
        run: uv sync --extra dev
      - name: Ruff
        run: uv run --no-build ruff check src tests
      - name: MCP config secret scan
        run: uv run --no-build python scripts/scan_mcp_secrets.py .mcp.json .cursor/mcp.json
      - name: Pytest
        run: uv run --no-build pytest -q
      - name: Build
        run: uv run --no-build python -m build

