# Examples

Example activation plans and execution contracts will be added here as the runtime proves stable across 2–3 real use cases.

See `runtime/kernel_pipeline/planner/KERNEL_ACTIVATION_PLAN.example.yaml` for the current canonical example.
