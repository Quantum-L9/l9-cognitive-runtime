version: 1
purpose: "Consumer-repo skill autonomy registry (empty — skills live in Cursor-Governance SSOT)."
principle: "This young package repo does not host L9 skills; discovery is via ~/.cursor-governance."

cursor_discovery:
  enabled_by: []
  automatic_invocation:
    rule: "n/a — no repo-local skills"
  explicit_invocation:
    rule: "n/a — no repo-local skills"

tiers:
  auto_invoke: []
  explicit_only: []

routing: {}
